export function Workout(name, bodyPart, sets, reps, weight, duration) {
  this.name = name;
  this.bodyPart = bodyPart;
  this.sets = sets;
  this.reps = reps;
  this.weight = weight;
  this.duration = duration;
}
